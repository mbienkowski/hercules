{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": [
    "Mutates the builder/release engine only — never src/metrics/ (a measuring tool, not shipped",
    "logic) or dist/ (generated output; proven by the drift gate, not mutation). mutmut covers the",
    "Python islands via `make mutation-py`; both read src/release/mutation-gate.json, whose threshold",
    "is 0 — these runs report a kill rate, they gate nothing and no CI job runs them."
  ],
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.mts"
  },
  "reporters": [
    "progress",
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": "reports/mutation/mutation.json"
  },
  "coverageAnalysis": "all",
  "mutate": [
    "src/builder/**/*.mts",
    "!src/builder/bin/**",
    "src/release/**/*.mts",
    "!src/release/bin/**"
  ],
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true,
  "timeoutMS": 60000,
  "concurrency": 4
}
