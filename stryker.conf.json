{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": [
    "Mutates the builder/release engine only — never src/metrics/ (a measuring tool, not shipped",
    "logic) or dist/ (generated output; proven by the drift gate, not mutation). mutmut covers",
    "src/hooks/ in the parallel mutation-py job; both read the one gate at src/release/mutation-gate.json."
  ],
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.config.mts"
  },
  "reporters": [
    "progress",
    "clear-text",
    "json"
  ],
  "jsonReporter": {
    "fileName": "reports/mutation/mutation.json"
  },
  "coverageAnalysis": "all",
  "mutate": [
    "src/builder/**/*.mts",
    "!src/builder/bin/**",
    "src/release/**/*.mts",
    "!src/release/bin/**"
  ],
  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true,
  "timeoutMS": 60000,
  "concurrency": 4
}
